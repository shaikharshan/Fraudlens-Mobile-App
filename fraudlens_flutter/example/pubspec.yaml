name: fraudlens_flutter_example
description: Example app for fraudlens_flutter — run flutter create here if Android/iOS folders are missing.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fraudlens_flutter:
    path: ../

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
